<!DOCTYPE html>
<html lang="en">
    <head>
        <title>LTI 1.3 Tool</title>
        
        <meta charset="utf-8">
        <meta name="description" content="LTI 1.3 Tool">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    
        <base href="/">
    </head>
    <body>
        <h2> LTI 1.3 Tool Launch successful</h2>
        <h3>List of Parameters recieved</h3>
        <ul>
            <% Object.keys(params).forEach(function(key) { %>
                <li><%= key %> : JSON.stringify(params[key] %>
                
                    <% if( typeof params[key] == "object") {%>
                       <%- JSON.stringify(params[key]) %> 
                    <% } %>
                </li>
            <% }); %>
        </ul>
    </body>
</html>